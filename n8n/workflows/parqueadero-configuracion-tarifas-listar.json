{
  "name": "Parqueadero - Configuración listar tarifas",
  "nodes": [
    {
      "parameters": { "httpMethod": "GET", "path": "panel/parqueadero/configuracion/tarifas", "responseMode": "responseNode", "options": {} },
      "id": "d1000001-0001-4001-8001-000000000001", "name": "Webhook", "type": "n8n-nodes-base.webhook", "typeVersion": 2, "position": [-500, 0], "webhookId": "panel-parqueadero-configuracion-tarifas"
    },
    {
      "parameters": { "jsCode": "const crypto=require('crypto'); const fail=(statusCode,error)=>[{json:{ok:false,statusCode,error}}]; const authorization=$json.headers?.authorization??''; if(!authorization.startsWith('Bearer ')) return fail(401,'Sesión requerida.'); const secret=$env.JWT_SECRET; if(!secret) return fail(500,'JWT_SECRET no está configurado.'); try { const [h,p,s,x]=authorization.slice(7).trim().split('.'); if(!h||!p||!s||x) throw new Error(); const expected=crypto.createHmac('sha256',secret).update(`${h}.${p}`).digest('base64url'); const l=Buffer.from(s),r=Buffer.from(expected); if(l.length!==r.length||!crypto.timingSafeEqual(l,r)) throw new Error(); const payload=JSON.parse(Buffer.from(p,'base64url').toString('utf8')); if(!payload.exp||payload.exp<=Math.floor(Date.now()/1000)) throw new Error(); const role=String(payload.rol??payload.role??'').toUpperCase(); const permissions=(Array.isArray(payload.permisos)?payload.permisos:[]).map(v=>String(v).toLowerCase()); if(role!=='SUPERADMIN'&&!permissions.includes('parqueadero.gestionar')) return fail(403,'No tiene permisos para configurar parqueadero.'); return [{json:{ok:true}}]; } catch { return fail(401,'La sesión es inválida o ha vencido.'); }" },
      "id": "d1000001-0001-4001-8001-000000000002", "name": "Autorizar configuración", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [-280, 0]
    },
    {
      "parameters": { "conditions": { "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" }, "conditions": [{ "id": "ok", "leftValue": "={{ $json.ok }}", "rightValue": true, "operator": { "type": "boolean", "operation": "equals" } }], "combinator": "and" }, "options": {} },
      "id": "d1000001-0001-4001-8001-000000000003", "name": "¿Autorizado?", "type": "n8n-nodes-base.if", "typeVersion": 2, "position": [-60, 0]
    },
    {
      "parameters": { "operation": "executeQuery", "query": "SELECT id::int AS \"id\", modalidad AS \"modality\", tipo_vehiculo AS \"vehicleType\", COALESCE(precio, 0)::double precision AS \"amount\", activo AS \"active\", actualizado_en::text AS \"updatedAt\"\nFROM public.parqueadero_modalidades\nORDER BY modalidad, tipo_vehiculo;", "options": {} },
      "id": "d1000001-0001-4001-8001-000000000004", "name": "Consultar tarifas (Postgres)", "type": "n8n-nodes-base.postgres", "typeVersion": 2.6, "position": [180, -100], "alwaysOutputData": true,
      "credentials": { "postgres": { "id": "REEMPLAZAR", "name": "YaviBot Postgres" } }
    },
    {
      "parameters": { "jsCode": "const data=$input.all().map(i=>i.json).filter(i=>i.id!=null); return [{json:{data}}];" },
      "id": "d1000001-0001-4001-8001-000000000005", "name": "Formatear respuesta", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [420, -100]
    },
    {
      "parameters": { "respondWith": "json", "responseBody": "={{ $json }}", "options": { "responseCode": 200 } },
      "id": "d1000001-0001-4001-8001-000000000006", "name": "Responder tarifas", "type": "n8n-nodes-base.respondToWebhook", "typeVersion": 1.4, "position": [650, -100]
    },
    {
      "parameters": { "respondWith": "json", "responseBody": "={{ { error: $json.error } }}", "options": { "responseCode": "={{ $json.statusCode }}" } },
      "id": "d1000001-0001-4001-8001-000000000007", "name": "Responder error", "type": "n8n-nodes-base.respondToWebhook", "typeVersion": 1.4, "position": [180, 120]
    }
  ],
  "connections": {
    "Webhook": { "main": [[{ "node": "Autorizar configuración", "type": "main", "index": 0 }]] },
    "Autorizar configuración": { "main": [[{ "node": "¿Autorizado?", "type": "main", "index": 0 }]] },
    "¿Autorizado?": { "main": [[{ "node": "Consultar tarifas (Postgres)", "type": "main", "index": 0 }], [{ "node": "Responder error", "type": "main", "index": 0 }]] },
    "Consultar tarifas (Postgres)": { "main": [[{ "node": "Formatear respuesta", "type": "main", "index": 0 }]] },
    "Formatear respuesta": { "main": [[{ "node": "Responder tarifas", "type": "main", "index": 0 }]] }
  },
  "pinData": {}, "settings": { "executionOrder": "v1" }, "active": false
}
